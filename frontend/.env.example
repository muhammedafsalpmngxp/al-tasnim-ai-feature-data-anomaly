# Only needed when the UI is served from a DIFFERENT origin than the API. During development
# the Vite proxy in vite.config.ts handles it, so leaving these blank is correct.
VITE_API_BASE=
# Must match API_KEY in backend/.env. Leave blank when the backend has no API_KEY set.
VITE_API_KEY=
